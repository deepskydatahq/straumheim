server:
  host: 0.0.0.0
  port: 8080

inputs:
  webhook:
    enabled: true
    path: /webhook

buffer:
  type: memory
  capacity: 1000
  flush_interval: 1s
  flush_count: 1

sinks:
  - name: debug
    type: stdout
    mode: stream
