# Domain for Caddy auto-TLS (e.g. events.example.com)
DOMAIN=events.example.com

# Postgres connection string for Straumheim
POSTGRES_DSN=postgres://straumheim:CHANGEME@postgres:5432/events?sslmode=disable

# Postgres password (used by the postgres container)
POSTGRES_PASSWORD=CHANGEME
